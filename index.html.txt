<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Apna Ward, Apni Awaaz | Bridging the Gap</title>

<script src="https://cdn.tailwindcss.com"></script>
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">

<style>
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&family=Merriweather:ital,wght@1,300&display=swap');

body {
    font-family: 'Inter', sans-serif;
    background: #f8fafc;
    color: #1e293b;
}

/* ---------- Reveal Animation ---------- */
.reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s ease;
}
.reveal.active {
    opacity: 1;
    transform: translateY(0);
}

/* ---------- Phone Mockup ---------- */
.phone-mockup {
    width: 300px;
    height: 600px;
    border-radius: 40px;
    border: 12px solid #1e293b;
    background: #fff;
    overflow: hidden;
    position: relative;
    box-shadow: 0 25px 50px rgba(0,0,0,0.3);
}
.notch {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 120px;
    height: 25px;
    background: #1e293b;
    border-bottom-left-radius: 12px;
    border-bottom-right-radius: 12px;
    z-index: 10;
}

/* ---------- Screens ---------- */
.screen {
    display: none;
    padding: 20px;
    height: 100%;
    animation: fade 0.4s ease;
}
.screen.active { display: block; }

@keyframes fade {
    from { opacity: 0; transform: scale(0.97); }
    to { opacity: 1; transform: scale(1); }
}

/* ---------- Ripple Button ---------- */
.btn-ripple {
    position: relative;
    overflow: hidden;
}
.btn-ripple:active::after {
    content: "";
    position: absolute;
    width: 200%;
    height: 200%;
    background: rgba(255,255,255,0.4);
    border-radius: 50%;
    top: -50%;
    left: -50%;
    animation: ripple 0.6s ease;
}
@keyframes ripple {
    to { opacity: 0; }
}

/* ---------- Toast ---------- */
.toast {
    position: fixed;
    bottom: 30px;
    right: 30px;
    background: #16a34a;
    color: white;
    padding: 14px 20px;
    border-radius: 12px;
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.4s ease;
    z-index: 999;
}
.toast.show {
    opacity: 1;
    transform: translateY(0);
}
</style>
</head>

<body>

<!-- Toast -->
<div id="toast" class="toast">
    ✅ Complaint successfully registered
</div>

<!-- ---------- HERO ---------- -->
<section class="min-h-screen flex items-center justify-center text-center px-6 reveal">
    <div>
        <h1 class="text-5xl font-extrabold mb-6">
            Connecting the <span class="text-blue-600">Pothole</span> to the <span class="text-teal-500">Policy</span>
        </h1>
        <p class="italic text-gray-500 text-xl">
            “From auto driver to Nagar Sevak — instantly.”
        </p>
    </div>
</section>

<!-- ---------- SOLUTION ---------- -->
<section class="py-24 bg-slate-900 text-white">
<div class="max-w-6xl mx-auto grid md:grid-cols-2 gap-12 px-6 items-center">

<!-- LEFT -->
<div class="reveal">
    <h2 class="text-4xl font-bold mb-6">Apna Ward, Apni Awaaz</h2>
    <p class="text-slate-300 mb-8">
        Report civic issues in under 60 seconds using photo + location.
    </p>

    <div class="space-y-6">
        <div class="flex gap-4">
            <div class="w-10 h-10 bg-blue-600 rounded-full flex items-center justify-center">1</div>
            <p>Click photo & auto geo-tag</p>
        </div>
        <div class="flex gap-4">
            <div class="w-10 h-10 bg-teal-500 rounded-full flex items-center justify-center">2</div>
            <p>Complaint routed to Nagar Sevak</p>
        </div>
        <div class="flex gap-4">
            <div class="w-10 h-10 bg-green-500 rounded-full flex items-center justify-center">3</div>
            <p>Track & verify resolution</p>
        </div>
    </div>
</div>

<!-- RIGHT (PHONE) -->
<div class="reveal flex justify-center">
<div class="phone-mockup">
<div class="notch"></div>

<!-- SCREEN 1 -->
<div id="screen1" class="screen active bg-gray-50 pt-12">
    <h3 class="font-bold text-lg mb-4">Report Issue</h3>
    <div class="h-32 bg-gray-200 rounded mb-4 flex items-center justify-center">
        <i class="fas fa-camera text-2xl text-gray-400"></i>
    </div>

    <button onclick="submitComplaint()" 
    class="btn-ripple w-full bg-blue-600 text-white py-3 rounded-lg font-medium">
        Submit Complaint
    </button>
</div>

<!-- SCREEN 2 -->
<div id="screen2" class="screen bg-white pt-16 text-center">
    <div class="text-teal-600 text-4xl mb-4 animate-bounce">
        <i class="fas fa-paper-plane"></i>
    </div>
    <h4 class="font-bold text-lg">Submitted</h4>
    <p id="ticketText" class="text-sm text-gray-500 mt-2"></p>

    <div class="mt-6">
        <div class="bg-gray-200 h-2 rounded">
            <div id="progressBar" class="bg-yellow-400 h-2 rounded w-[30%] transition-all"></div>
        </div>
        <p id="statusText" class="text-xs mt-2 text-yellow-600">Under Review</p>
    </div>
</div>

<!-- SCREEN 3 -->
<div id="screen3" class="screen bg-gray-50 pt-12 text-center">
    <div class="text-green-600 text-4xl mb-4">
        <i class="fas fa-check-circle"></i>
    </div>
    <h4 class="font-bold text-lg">Issue Resolved</h4>
    <p class="text-xs text-gray-500">Pothole repaired successfully</p>

    <button onclick="showScreen('screen1')" 
    class="mt-6 w-full bg-gray-300 py-2 rounded">
        Report Another Issue
    </button>
</div>

</div>
</div>

</div>
</section>

<script>
/* Reveal */
window.addEventListener('scroll', () => {
document.querySelectorAll('.reveal').forEach(el => {
if (el.getBoundingClientRect().top < window.innerHeight - 150) {
el.classList.add('active');
}
});
});

/* Screen Change */
function showScreen(id) {
document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
document.getElementById(id).classList.add('active');
}

/* Submit Logic */
function submitComplaint() {
const ticket = "WARD-" + Math.floor(100 + Math.random() * 900);
document.getElementById("ticketText").innerHTML =
`Ticket <strong>#${ticket}</strong> sent to Nagar Sevak`;

showScreen('screen2');
showToast();

/* Simulate progress */
setTimeout(() => {
document.getElementById("progressBar").style.width = "70%";
document.getElementById("statusText").innerText = "Work in Progress";
}, 2000);

setTimeout(() => {
document.getElementById("progressBar").style.width = "100%";
document.getElementById("statusText").innerText = "Resolved";
}, 4000);

setTimeout(() => {
showScreen('screen3');
}, 5500);
}

/* Toast */
function showToast() {
const toast = document.getElementById("toast");
toast.classList.add("show");
setTimeout(() => toast.classList.remove("show"), 3000);
}
</script>

</body>
</html>
